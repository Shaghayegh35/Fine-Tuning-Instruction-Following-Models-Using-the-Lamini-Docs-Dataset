# Fine-Tuning-Instruction-Following-Models-Using-the-Lamini-Docs-Dataset
This project demonstrates how to preprocess, format, and fine-tune a language model using the Lamini Docs dataset, which contains instruction–response pairs designed to train models that can follow natural language instructions (similar to ChatGPT-style behavior).
